<%- include('partials/header'); -%>


    <h1>Home</h1>
    <p> <%= homeText%> </p>
    
     <% console.log(bothInputs) %>

     <% bothInputs.forEach((input)=>{ %>
        <!-- <%console.log(input.title)%> -->
        
        <h1><%=input.title%></h1>

        <% if (input.content.length > 100) {%>
            
            <% input1 = input.content.substring(0, 99) + "...";%>
            <p><%=input1%>
                <a href= "/posts/<%=input.title%>">Read More</a>
            </p>

            <%  }else if(input.content.length < 100){%>
                <p><%=input.content%></p>
                <% }%>
        
        <%})%>
          

        <!-- <%for(i=0; i < bothInputs.length; i++){%>
            <%console.log(bothInputs[i].title)%>   
     <%};%> -->

<%- include('partials/footer'); -%>